parameters:
  - $ref: "#/parameters/resourceId"
get:
  tags:
    - Tracking
    - Coupons
  summary: Retrieve a tracking coupon's redeem log with specified identifier string
  responses:
    200:
      description: Tracking coupon's redeem log was retrieved successfully
      schema:
        $ref: "#/definitions/CouponTracking"
    404:
      $ref: "#/responses/NotFound"
    401:
      $ref: "#/responses/AccessForbidden"
