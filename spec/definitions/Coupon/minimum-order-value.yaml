description: minimum-order-value restrictions
allOf:
  -
    $ref: "#/definitions/CouponRestriction"
  -
    type: "object"
    required:
     - "value"
    properties:
      value:
        type: string
        description: Restriction value #todo
        enum:
          - currency
          - amount
