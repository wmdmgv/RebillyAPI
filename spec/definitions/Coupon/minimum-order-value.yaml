description: minimum-order-value restrictions
allOf:
  -
    $ref: "#/definitions/CouponRestriction"
  -
    type: "object"
    required:
     - "amount"
     - "currency"
    properties:
      currency:
        type: string
        description: Restriction currency
      amount:
        type: string
        description: Restriction amount
