description: Redemption additional restrictions
type: object
discriminator: "type"
properties:
  type:
    description: Redemption additional restriction
    type: string
    enum:
      - restrict-to-invoices
      - restrict-to-subscriptions
