description: Coupon fixed discount
allOf:
  -
    $ref: "#/definitions/Discount"
  -
    type: "object"
    required:
     - "value"
     - "currency"
    properties:
      value:
        description: Discount value
        type: number
        format: double
      currency:
        description: Discount currency
        type: string
