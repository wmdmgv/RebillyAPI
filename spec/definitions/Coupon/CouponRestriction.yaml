description: Coupon restrictions
type: object
discriminator: "type"
properties:
  type:
    description: Coupon's restriction type
    type: array
    items:
      type: string
      enum:
        - fixed-use-count
        - minimum-order-value
        - apply-to-products
        - restrict-to-plans
        - restrict-to-products
        - total-quantity
        - limit-per-customer
