description: Coupon restrictions
type: object
discriminator: "type"
properties:
  type:
    description: Coupon's restriction type
    type: array
    items:
      type: string
      enum:
        - discounts-per-customers
        - discounts-per-redemptions
        - minimum-order-amount
        - restrict-to-plans
        - restrict-to-products
        - total-quantity
