type: object
allOf:
  - $ref: "#/definitions/NewLink"
properties:
  rel:
    description: The link type
    type: string
    enum:
      - dispute
      - customer
      - payment
      - transaction
      - subscription
      - plan
      - product
      - invoice
required:
  - rel
