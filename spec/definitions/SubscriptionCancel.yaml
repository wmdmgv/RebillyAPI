type: object
required:
  - policy
properties:
  policy:
    description: Cancel policy
    type: string
    enum:
      - at-next-renewal
      - now-with-prorata-refund
      - now-with-prorata-credit
      - now-without-refund
      - now-with-full-refund
